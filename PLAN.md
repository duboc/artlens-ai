# setup.sh — GCP Project Bootstrap Script

## Checklist

- [x] **1 — Create `setup.sh`** with full GCP bootstrap logic: prerequisites check, project resolution, API enablement, Firestore creation, GCS bucket creation, ADC setup, `.env.local` generation, summary output.
- [x] **2 — Update `.env.example`** with descriptions for all env vars matching `server/config.ts`.
- [x] **3 — Verify** script is executable and well-formed.
